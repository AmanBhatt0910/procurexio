// src/config/vendors.js
//
// Vendor-related constants used across the vendor list,
// detail, and new-vendor pages.

/**
 * Valid vendor status values — matches the vendors.status DB ENUM.
 * Order here is the canonical display order (pending first, then active, then inactive).
 */
export const VENDOR_STATUSES = ['pending', 'active', 'inactive'];
