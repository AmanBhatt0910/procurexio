/**
 * src/lib/database/index.js
 *
 * Barrel re-export for the database connection pool.
 *
 * @example
 * import pool from '@/lib/database';
 * // or
 * import { pool } from '@/lib/database';
 */

export { default, default as pool } from '../db';
